apiVersion: v1
kind: Service
metadata:
  name: zenwebapi9-service
  labels:
    app: zenwebapi9
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: zenwebapi9
